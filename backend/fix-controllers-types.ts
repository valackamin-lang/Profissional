/**
 * Script para corrigir tipos dos controllers
 * Este script ajuda a identificar controllers que precisam ser atualizados
 */

// Este arquivo é apenas para referência
// Os controllers devem ser atualizados manualmente para usar Request em vez de AuthRequest
// e fazer cast interno: const authReq = req as AuthRequest;

export {};
